Task1 : 
    Programm for Even and Odd check which uses the modulus operator to check for even/odd based on reminder.


Task2 : 
    programm for series addition which calculates the total of all the numbers from 1 to 50 and returns its total value.
    variables used is the "total" which behaves like a flag value which updates with each execution till the loop ends.
    